services:
  # MinIO S3-compatible object storage
  minio:
    image: minio/minio:latest
    environment:
      MINIO_ROOT_USER: minioadmin
      MINIO_ROOT_PASSWORD: minioadmin
    command: minio server /data --console-address ":9001"
    volumes:
      - minio_data:/data
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:9000/minio/health/live"]
      interval: 5s
      timeout: 3s
      retries: 5

  # Create MinIO bucket and upload test database
  minio-setup:
    image: minio/mc:latest
    depends_on:
      minio:
        condition: service_healthy
    entrypoint: >
      /bin/sh -c "
      mc alias set minio http://minio:9000 minioadmin minioadmin;
      mc mb minio/test-deployments --ignore-existing;
      mc cp /fixtures/test-database.sql.gz minio/test-deployments/test-db.sql.gz;
      echo 'MinIO setup complete';
      "
    volumes:
      - ./fixtures/test-database.sql.gz:/fixtures/test-database.sql.gz

  # MySQL database
  mysql:
    image: mysql:8.0
    command:
      - --innodb-buffer-pool-size=64M
      - --innodb-log-buffer-size=8M
      - --key-buffer-size=8M
      - --max-connections=50
      - --performance-schema=OFF
    environment:
      MYSQL_DATABASE: drupaldb
      MYSQL_USER: drupal
      MYSQL_PASSWORD: drupal_password
      MYSQL_ROOT_PASSWORD: root_password
    volumes:
      - mysql_data:/var/lib/mysql
    healthcheck:
      test: ["CMD-SHELL", "mysqladmin ping -h 127.0.0.1 -uroot -proot_password --silent"]
      start_period: 60s
      interval: 5s
      timeout: 5s
      retries: 36

  # Test origin server (simple web server with test files)
  origin-server:
    image: python:3.11-slim
    volumes:
      - ./fixtures/origin-files:/files
    working_dir: /files
    command: python -m http.server 8000
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8000/')"]
      interval: 5s
      timeout: 3s
      retries: 5

  # Prepare the fixture app for deployment startup.
  # This one-shot step initializes missing fixture root files and applies
  # ownership/permission adjustments needed for bootstrap/composer writes.
  app-fixture-prepare:
    image: test-df-deployment:8.3
    build:
      context: ..
      dockerfile: Dockerfile
      args:
        PHP_VERSION: 8.3
    env_file:
      - .env.shared
    user: root
    # $$ escapes Compose variable interpolation so the shell receives a literal $.
    # See https://docs.docker.com/reference/compose-file/interpolation/
    entrypoint:
      - /bin/bash
      - -lc
      - >
        if [ ! -f /var/www/html/composer.json ]; then
          case "$$DP_REPO_BRANCH" in
            */-/tree/*)
              repo_url="$${DP_REPO_BRANCH%%/-/tree/*}.git";
              repo_branch="$${DP_REPO_BRANCH##*/-/tree/}";
              ;;
            */tree/*)
              repo_url="$${DP_REPO_BRANCH%%/tree/*}.git";
              repo_branch="$${DP_REPO_BRANCH##*/tree/}";
              ;;
            *)
              echo "Invalid DP_REPO_BRANCH: $$DP_REPO_BRANCH";
              echo "Expected .../tree/<branch> (GitHub) or .../-/tree/<branch> (GitLab)";
              exit 1;
              ;;
          esac;
          rm -rf /var/www/html/* /var/www/html/.[!.]* /var/www/html/..?* 2>/dev/null || true;
          git clone --branch "$$repo_branch" --single-branch --depth 1 \
            "$$repo_url" /var/www/html;
          cd /var/www/html;
          composer require drush/drush drupal/stage_file_proxy --no-interaction;
        fi;
        if [ ! -f /var/www/html/web/sites/default/settings.php ] &&
           [ -f /var/www/html/web/sites/default/default.settings.php ]; then
          cp /var/www/html/web/sites/default/default.settings.php /var/www/html/web/sites/default/settings.php;
        fi;
        mkdir -p /var/www/html/web/sites/default/files &&
        (chmod -R a+rwX /var/www/html/web/sites/default/files 2>/dev/null || true) &&
        (chown -R www:www /var/www/html 2>/dev/null || true)
    volumes:
      - ./fixtures/app:/var/www/html

  # Deployment container - runs the full initialization
  deployment:
    image: test-df-deployment:8.3
    depends_on:
      app-fixture-prepare:
        condition: service_completed_successfully
      mysql:
        condition: service_healthy
      minio-setup:
        condition: service_completed_successfully
      origin-server:
        condition: service_healthy
    env_file:
      - .env.shared
      - .env.test
    volumes:
      - ./fixtures/app:/var/www/html
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost/"]
      start_period: 120s
      interval: 5s
      timeout: 3s
      retries: 48

  # Secure deployment container - runs Apache as its default www-data user (no APACHE_RUN_USER/GROUP set)
  # Both deployment and deployment-secure mount the same volume; FILE_PROXY_PATHS uses a distinct path
  # so both containers can create/chown their own proxy directories without interfering with each other.
  # A named volume is used for the proxy path so that chown to www-data works correctly on all
  # platforms, including macOS where bind-mount ownership changes may not persist reliably.
  deployment-secure:
    image: test-df-deployment:8.3
    depends_on:
      deployment:
        condition: service_healthy
      mysql:
        condition: service_healthy
      origin-server:
        condition: service_healthy
    environment:
      # Skip bootstrap and database import for this scenario
      BOOTSTRAP_REQUIRED: "no"
      APP_ROOT_TIMEOUT: "0"
      APP_ROOT: /tmp/nonexistent-app
      USE_STAGE_FILE_PROXY: "no"

      # Include both paths so secure startup does not overwrite the default path
      # rewrite when both containers share the same .htaccess on the app volume.
      ORIGIN_URL: http://origin-server:8000
      FILE_PROXY_PATHS: /sites/default/files,/sites/www-data-test-files
      WEB_ROOT: /var/www/html/web
    volumes:
      - ./fixtures/app:/var/www/html
      - secure_proxy_files:/var/www/html/web/sites/www-data-test-files

volumes:
  minio_data:
  mysql_data:
  secure_proxy_files:
